type User {
    userId: String!
    username: String!
    created: String!
    reservations: [Reservation]
    status: UserStatus
}

input CreateUserInput {
    username: String!
    password: String!
}

enum UserStatus {
    ACTIVE
    DELETED
    ADMIN
}
