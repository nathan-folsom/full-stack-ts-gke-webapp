type Reservation {
    id: String!
    userId: String!
    time: String!
    location: String!
}

extend type Query {
    allReservations(userId: String!): [Reservation]
    myReservations(userId: String!): [Reservation]
}